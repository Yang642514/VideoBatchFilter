# 最小化依赖 - 基本功能
pandas>=0.23.0              # Excel/CSV文件处理 (当前已安装0.23.4)
openpyxl>=2.5.0             # Excel文件读写支持 (当前已安装2.5.6)
tqdm>=4.26.0                # 进度条显示 (当前已安装4.26.0)
requests>=2.20.0            # HTTP请求，用于网络操作

# 注意：yt-dlp需要编译器支持，如果安装失败可以跳过
# 在没有yt-dlp的情况下，系统仍可以处理已有的视频信息