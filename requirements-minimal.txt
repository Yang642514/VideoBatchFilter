# Minimal dependencies for video batch filter
# Usage: pip install -r requirements-minimal.txt

# Core dependencies (required)
yt-dlp>=2023.11.16
tqdm>=4.50.0
requests>=2.25.0

# Data processing (recommended for CSV/Excel processing)
pandas>=1.1.0
openpyxl>=3.0.0

# Network and retry support
urllib3>=1.26.0